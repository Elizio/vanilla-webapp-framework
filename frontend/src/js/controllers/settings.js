/** Placeholder settings page controller. */
export const settingsController = {
    appContext: null,

    /** @param {object} appContext */
    init(appContext) {
        this.appContext = appContext;
    },
};
